# Hard Constraints — the redesign MUST preserve all of these

- INTEGRATION TEST (integration_test/app_smoke_test.dart): exactly one MemShell widget and exactly one Material 3 NavigationBar must exist (no BottomNavigationBar/custom nav); Icons.add_circle_outline, Icons.chat_bubble_outline, and Icons.article_outlined must remain findable and tappable as the tab-switch affordances; root wiring must stay AppScope(state: AppState(), child: MemDroidApp()) with those class names.
- SHELL: MemShell hosts the 3 tabs in an IndexedStack (Notes=0, Capture=1, Chat=2) keeping all alive — Capture drafts/toggles/in-progress recordings and Chat state must survive tab switches; shellTabRequest is consumed-once-and-cleared; deep links memai://open → tab 0 and memai://prompt?templateId=<id> → tab 2 + enqueue; MemJobNotifications.onOpenHomeTab → goToShellTab(0); syncHomePromptWidget re-runs at shell startup.
- CHAT PROMPT QUEUE: MemShell creates ONE ChatPromptQueue and injects it into MemChatPage — instance identity must be preserved or widget-tap jobs are lost; enqueue() adds then notifyListeners; drainAll() is atomic copy-then-clear (second listener must see empty); FIFO serial processing with _drainingPromptRuns re-entrancy guard; QueuedPromptJob semantics: text = verbatim LLM prompt, notificationTitle = notification label, notifyOnComplete gates notification.
- APPSTATE CONTRACTS: keep AppScope (InheritedNotifier) as-is or migrate wholesale (doc warns against half-swaps); AppState holds no server data — screens own fetch/loading/error/pagination state; notesListRevision ValueNotifier is the sole cross-screen invalidation channel — every mutation must keep bumping it (capture memIt + createNote, note save/trash/restore/hard-delete, collection create/update/delete) and NotesPage must keep its attach-on-instance-change/detach-old/detach-in-dispose listener pattern; isHydrated gates initial loads (a failed load() leaves it false forever — shell must render sensibly pre-hydration); hasMemRest gates Notes/Collections features and the filter bar must not render without it.
- API CLIENT LIFECYCLE: MemApiClient is constructed fresh per operation from the CURRENT memApiKey (never cached; null/empty key → null client) so key changes apply immediately; NotesPage call shape: rawListNotes(limit 40, includeContent false, page cursor or null on refresh, collectionId filter), next_page kept only when String; day grouping key = DateFormat.yMMMEd on updatedAt.toLocal().
- NOTES PAGE: idempotent lazy initial load (guards: isHydrated && hasMemRest, not loading, not already loaded) reachable from initState post-frame, didChangeDependencies, and app-state changes; 'All' chip = null filter; _loadCollections auto-clears a filter whose collection vanished; refresh clears items+cursor pre-fetch; load-more only when cursor != null and calls without refresh; RefreshIndicator + AlwaysScrollableScrollPhysics (works on short content); manage-collections push is awaited then reloads collections + notes; card tap passes noteId only; MemErrorReporter context string 'notes_load'.
- COLLECTIONS MANAGE: AppScope access deferred to post-frame; delete sequence = deleteCollection → await reload → mounted check → bumpNotesListRevision → snackbar (no bump on failure; AppState captured before awaits); create/update payload asymmetry is intentional API behavior: createCollection sends description null when empty, updateCollection sends the trimmed (possibly empty) string to clear it; editor sheet save ordering: capture page ScaffoldMessenger BEFORE pop, pop sheet via sheetContext, then await onSaved (page reload + bump), sheet stays open on error; resolveClient injected as a function (never snapshot a client into the sheet); plain Navigator.pop with no result must remain sufficient (NotesPage awaits the push).
- CAPTURE: memIt sends RAW untrimmed body (trim only for emptiness guard), saveRaw sends raw markdown; fields clear only on success; instructions trimmed→null, cleared only on memIt success; transcript insertion: replace when _replaceMode or body blank, else existing.trimRight() + '\n\n' + normalized; auto-punctuate gate and 'No speech detected.' empty-result path; OpenAI key resolution order exactly: voiceOpenAiApiKey → active chat model if provider=='openai' → first openai model → vault.getLlmApiKey(profileId); recording: hasPermission check, AAC-LC 128kbps/44.1kHz to '<tmp>/capture_<epochMs>.m4a', stop uses (returned path ?? _activeRecordPath), temp file deleted in finally regardless of outcome, mic-mode switch cancels recording; amplitude normalization ((db+60)/60).clamp(0,1) at 120ms; haptics medium/light on hold start/end; disable matrix: mic blocked by busy||transcribing, saves blocked only by busy; bumpNotesListRevision after every successful memIt and createNote; dispose cancels timer/subscription, disposes recorder + 4 controllers.
- CHAT PIPELINE: send order = trim → busy gate → profile gate → backend gate (!hasMemRest && !mcpConnected aborts) → vault key gate → insert user msg → insert pending '…' bubble → refreshMcpIfNeeded → MemToolRunner (REST preferred; MCP client only when !hasMemRest && mcpConnected) → hasBackend check → dispatch on literal 'openai'|'anthropic'|'gemini'; streaming rewrites the pending bubble in place (same id); '…' is the sentinel filtered out on persist; provider histories replaced wholesale from results with _openAiHist[0] always the system message (anthropic/gemini get system separately); HTTP 400 → _resetProviderHistory; _failMessage rewrites pending bubble with 'Error: ' prefix; finally: busy=false + unawaited persist when mounted.
- CHAT SESSIONS + JOBS: per-profile persistence (ChatSessionStore key 'chat_session_v1_<profileId>', 5-day retention, text-only round-trip); profile switch persists OLD session first, then restores (null → empty; reseed openAiHist system message if stored empty); prompt jobs POLL at 150ms while busy then run, user sends while busy snackbar-abort — both behaviors must survive; MemJobNotifications.showPromptJobFinished fires on BOTH success and failure but only when notifyTitle != null; MemErrorReporter context 'prompt_job' vs 'chat'; jobs picker onPick pops the sheet BEFORE _runFromTemplate; _activeProfile falls back to first model on id miss; active model + provider must stay visible somewhere; app listener removed against the cached _app reference, promptQueue listener removed, _chat disposed.
- NOTE DETAIL: post-frame _load; missing key → 'Missing Mem API key.' short-circuit; listCollections failure swallowed (chips fall back to raw id via _collectionTitle); optimistic concurrency — _version captured from every readNote/updateNote response and passed back; save silently no-ops without version/key; updateNote is ONE combined write of markdown + full collectionIds list; save success repopulates ALL state from the server response then bumps revision; save failure keeps edit mode + controller text; _beginEdit snapshots the collection set, _cancelEdit restores it; read mode MUST use MarkdownBody (not Markdown) with selectable:true — nested scrollable steals drags; both confirms are showDialog<bool> proceeding only on true && mounted; trash/restore reload in place (no pop), hard delete is the only pop (nav captured before await); AppBar action gating keyed on inTrash × _editing; 'first line becomes the title' is Mem API semantics, not just hint copy.
- SETTINGS: key prefills read post-frame; trim + empty→null semantics — an empty field DELETES the stored key (both Mem and voice); OAuth error taxonomy (UserCancelled → 'Sign-in cancelled.', PlatformException → 'OAuth error: <msg>', generic → toString); model editor: new profiles REQUIRE a key, blank key on edit keeps the vault entry; save order = capture messenger+AppScope from page context → pop dialog → vault.setLlmApiKey if key given → saveChatModels(activeId); profile id keys both ChatModelProfile and vault entry (existing id preserved, new = Uuid v4); active-model rules: add→new active, edit→keep, remove-active→promote first, remove-last→null; vault key deleted BEFORE saveChatModels on remove; displayName always recomputed via composeChatDisplayName (never user-editable); catalog-drift warning + provider-change reset behavior; editor keyCtrl intentionally NOT disposed (route-teardown rebuild crash); voice dropdown values exactly 'whisper-1' and 'gpt-4o-mini-transcribe'; PromptJobsPage must stay reachable from Settings.
- PROMPT JOBS: pinnedTemplateIds is the ORDER source of truth (max 4, deduped, stale ids tolerated silently via promptById+filter); pin flow syncHomePromptWidget BEFORE requestPinPromptWidget; reorder uses the standard newI>oldI decrement then persists the full list unawaited; ReorderableListView keys = ValueKey(t.id); 5th-pin rejection guard + snackbar; delete via showDialog<bool> with context.mounted between awaits; editor: id = existing?.id ?? Uuid v4 — template ids are STABLE contract for home-widget deep links and must never be renamed/regenerated on edit; pop sheet (messenger captured first) then await upsertPromptJob; _pinSupported resolved async post-frame and gates the pin button.
- EDITOR SHEETS (both): showModalBottomSheet must stay isScrollControlled with bottom padding MediaQuery.viewInsetsOf(context).bottom + 20 for keyboard avoidance; controllers seeded from existing and disposed in the sheet State; values trimmed on save.
- SETTINGS LAUNCHER: settingsIconActions(context) must keep returning List<Widget> compatible with AppBar.actions (three call sites), remain present on every primary tab, push const SettingsPage() (no arguments contract), and target the nearest Navigator so the route covers the NavigationBar.
- ASYNC HYGIENE (app-wide, load-bearing): ScaffoldMessenger.of(context) captured BEFORE every await; mounted (or dialogContext.mounted / sheetContext.mounted) checked after every await before setState/snackbar/pop; AppScope lookups deferred to post-frame in initState paths — any rebuild must preserve this pattern or introduce use-after-dispose crashes.
- TELEMETRY: MemErrorReporter.report context strings 'notes_load', 'chat', and 'prompt_job' are consumed downstream and must survive; error reports include provider and httpStatus in chat paths.
- LAYOUT CHROME: list content must keep clearing the shell NavigationBar (Notes currently uses bottom inset 120) and FABs (PromptJobs bottom 96) — values may change but the clearance requirement stands.

